def IsPrime(n):
    if n == 1: 
        return False  
    d = 2
    while d * d <= n and n % d != 0:
        d += 1
    return d * d > n

def sum_primes(arr):
    sum = 0
    for element in arr:
        if IsPrime(element):
            sum += element
    return None if sum == 0 else sum

